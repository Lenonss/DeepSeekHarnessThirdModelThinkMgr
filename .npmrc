# pnpm supply-chain policy: allow the @deepseek-ai rc.6 prerelease set used by
# dsh profiles (published recently; the default minimum-release-age window
# would reject them on fresh installs).
minimum-release-age=0
